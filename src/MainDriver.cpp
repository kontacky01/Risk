#include "Player.h"
#include <iostream>

using namespace std;


int main()
{
  // PlayerDriver *p = new PlayerDriver();
  // p->testPlayers();
  PlayerDriver *p = new PlayerDriver();
  p->testPlayers();
  cout << "Hello this is our first file";
  return 0;
}